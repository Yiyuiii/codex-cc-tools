# 0.1.0 Release Review

Status: prepared for stable npm `latest` release through GitHub Actions Trusted Publishing.

Checked on: 2026-05-22

## Scope

This stable release promotes the validated `0.1.0-beta.1` release candidate to `0.1.0`.

## Verification

```bash
npm run verify:release
node dist/index.js doctor
node dist/index.js research --provider-profile deepseek --model deepseek-v4-flash --effort low --cwd D:\Codes\codex-cc-tools --include-git-status --include-files package.json,src/version.ts --question "DeepSeek CLI smoke before stable release. Read package.json and src/version.ts from the supplied evidence only. Report package version and exported VERSION. Do not modify files."
```

Results:

- `npm run verify:release`: passed locally with 33 test files and 163 tests.
- `node dist/index.js doctor`: passed.
- DeepSeek CLI smoke: passed with route target `api.deepseek.com` and token source `OPENAI_API_KEY_DEEPSEEK`.
- Codex MCP smoke: `cc_research` was callable from Codex after restart and returned structured output.

Expected release tag:

```bash
v0.1.0
```

## Notes

Stable release validation evidence is archived in `.release-validation/v0.1.0.md`.
