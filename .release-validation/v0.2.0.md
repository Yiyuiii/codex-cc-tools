# v0.2.0 Stable Release Validation

Checked on: 2026-05-22

RC: v0.2.0-beta.0

## Required Markers

Doctor: pass
Local-Codex-Smoke: pass
cc_review: pass

`cc_review` release diff review found no release blockers. It recorded one
procedural reminder: commit this validation file before creating the stable tag.

## Scope

`v0.2.0` promotes the `0.2.0-beta.0` next release after local Codex
validation. The public surface is intentionally reduced to:

- `cc_review`
- `cc_delegate`

The removed `research` and `verify` task families remain historical prerelease
work only. `cc_delegate` is now the autonomous Claude Code execution path:
Codex passes one complete `prompt`, and execution-space policy remains outside
the MCP tool.

## Local Validation

The maintainer completed a Codex restart and `@next` validation pass before
stable promotion approval. A follow-up DeepSeek MCP smoke was also run through
`cc_delegate` from Codex:

- provider profile: `deepseek`
- model: `haiku`
- marker: `DPSK_STABLE_PROMOTION_SMOKE`
- result: succeeded
- returned model: `deepseek-v4-flash`
- route diagnostic: `DeepSeek route target: api.deepseek.com; token source: OPENAI_API_KEY_DEEPSEEK.`

## Deterministic Verification

Run before stable version commit:

```bash
npm run typecheck
npm test
npm run release:smoke
node dist/index.js --version
node dist/index.js doctor
node dist/index.js delegate --help
git diff --check
```

Expected stable tag:

```bash
v0.2.0
```

## Notes

GitHub Actions release workflow publishes stable tags to npm `latest` only when
the tag is reachable from `main` and this validation file is present at the
tagged commit.
